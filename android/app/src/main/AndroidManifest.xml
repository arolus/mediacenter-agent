<?xml version="1.0" encoding="utf-8"?>
<!-- MediaCenter for Android/Google TV: the WebView UI *and* the node agent in one APK.
     Same package and signing key as the old WebView-only app - installs as an upgrade. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Scanning shared storage when mediaRoot points there (box); app-private dirs need none -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- Телефон-нода: медиатека лежит в общей памяти (/storage/emulated/0/Movies…), и добраться
         до чужих файлов на Android 11+ можно ТОЛЬКО так. Выдаётся вручную в настройках, один раз;
         приложение ставится сбоку, поэтому политика Google Play здесь ни при чём. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <!-- Android TV home-screen rows: Watch Next + own channel (HomeScreen.java) -->
    <uses-permission android:name="com.android.providers.tv.permission.READ_EPG_DATA" />
    <uses-permission android:name="com.android.providers.tv.permission.WRITE_EPG_DATA" />

    <!-- Android 11+ прячет от приложения чужие пакеты, поэтому проверка «установлен ли VLC»
         молча возвращала «нет», и кнопка установки появлялась при уже стоящем плеере.
         Объявляем, что нам нужно видеть плеер и всё, что умеет открывать видео. -->
    <queries>
        <package android:name="org.videolan.vlc" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="video/*" />
        </intent>
    </queries>

    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <!-- allowNativeHeapPointerTagging=false: Android 11 включил «мечение указателей» в
         нативной куче на arm64, и libtorrent4j падал сразу при переносе — «Scudo ERROR:
         invalid chunk state when deallocating» (поймано на Galaxy S10+, Android 11).
         Телевизор переносы делал нормально: у него другая архитектура. -->
    <application
        android:label="MediaCenter TV"
        android:allowNativeHeapPointerTagging="false"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/banner"
        android:allowBackup="false"
        android:usesCleartextTraffic="true"
        android:theme="@style/TvTheme">

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden|density|screenLayout|smallestScreenSize|uiMode"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <!-- Optional HOME role (parental mode on the box) - assigned externally, never by default -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ClearanceActivity"
            android:label="rutracker"
            android:exported="true" />

        <!-- The node agent itself: Firebase, library scan, HTTP server, torrents -->
        <service
            android:name=".agent.AgentService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Boot into the agent without Termux:Boot -->
        <receiver android:name=".agent.BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".Screensaver"
            android:exported="true"
            android:label="MediaCenter"
            android:permission="android.permission.BIND_DREAM_SERVICE">
            <intent-filter>
                <action android:name="android.service.dreams.DreamService" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

        <service
            android:name=".KeyService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/keyfilter" />
        </service>

        <receiver
            android:name=".AdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
